# Зависимости для тестирования агентов (TutorAgent и EvaluatorAgent)
# Python 3.13+

# OpenAI API для работы с агентами
openai>=1.0.0

# Опционально: pytest для запуска тестов (если планируется использовать pytest)
pytest>=8.0
pytest-asyncio>=0.24

# Для работы с данными (если потребуется)
# pandas>=2.0.0  # Раскомментировать, если нужна работа с DataFrame
